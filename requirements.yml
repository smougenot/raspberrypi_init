# Requirements file for Ansible Galaxy roles
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: community.general
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.0.0"

roles:
  # Example roles that might be useful for Raspberry Pi
  - name: geerlingguy.docker
    version: "4.2.0"
  - name: geerlingguy.pip
    version: "2.1.0"
  
# You can add more roles as needed
